export type UserData = {
    username: string,
    email: boolean,
    emailVerified: boolean,
    displayName: string,
    deck?: string
    role: any,
    isAdmin: boolean,
    sessionToken: string
}